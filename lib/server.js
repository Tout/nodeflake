var net = require('net');

//Load dependencies
var http = require("http"),
    url = require("url");
var listen = require('./listen.js');
    
function serveSocket() {
  try {
    var server = net.createServer(config.sockFile, listen(c);
  }
}